Flask==2.3.2
Werkzeug==2.3.8
prometheus_flask_exporter==0.22.0 
# Using a specific version for prometheus_flask_exporter for stability, 
# you can use a newer one if preferred, but check for compatibility.
